import React, { FC } from 'react'
import { UpdateModalProps } from './UpdateModal.types'

const UpdateModal:FC<UpdateModalProps> = ({onClose, onUpdate, initialValue}) => {
    
  return (
    <div>

    </div>
  )
}

export default UpdateModal